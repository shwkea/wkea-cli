# 需求询价管理

## 1. 业务概念

**需求询价** — B2B 采购的核心入口。客户提需求 → 内部匹配产品/供应商 → 向供应商询价 → 供应商报价 → 转订单。

### 实体关系
```
DemandQuotation（需求询价主表）
  ├── DemandQuotationItem（行项目 1..N）
  │     └── skuId → SKU（可选绑定）
  ├── DemandQuotationDocInfo（供应商报价记录）
  │     └── DemandQuotationDocInfoData（报价明细）
```

### 状态
| 状态码 | 含义 |
|--------|------|
| 274 | 待处理 |
| 275 | 处理中 |
| 276 | 已完成 |
| 291 | 已取消 |

### 行项目关键字段
- 客户填写（只读）：`productName`、`productBrand`、`productModel`
- 后台编辑：`manageProductName`、`manageProductBrand`、`manageProductModel`
- 绑定产品：`skuId`
- 业务数据：`quantity`、`expectPrice`、`finalSkuPrice`、`grossMargin`
- 记录：`aiRemark`（AI 处理记录，Markdown 格式）

---

## 2. 前置条件

- 产品匹配前：需求已被当前 AI 领取（领取由 AI 自动处理）
- 供应商匹配前：行项目必须已绑定 SKU
- 询价前：行项目涉及的品牌必须有绑定的供应商

---

## 3. 判断依据

- **ES 搜索可用**（线上） → 优先用 ES 搜索已有产品
- **ES 不可用**（开发环境返回 500） → 走 WebSearch
- **品牌有供应商** → 直接询价；**品牌无供应商** → 必须网上搜索创建，不能跳过

---

## 4. 操作流程

### 4.0 创建需求（客户提出需求时）

当客户提出需求（文字描述或图片/文件），先解析需求内容，再创建到系统，然后自动进入处理流程。

#### Step 0.1：解析需求

使用 `parse_demand` 工具或 `demand parse` 命令将客户的需求文本/附件解析为结构化行项目：

**方式 A：使用 MCP 工具 `parse_demand`**
- 将客户文字描述/附件 URL 传入，返回结构化行项目
- text — 客户文字描述或表格文件 URL；customerRemark — 需求额外备注；annex — 附件 URL（逗号分隔）

**方式 B：使用 CLI 命令 `demand parse`**
- 先 `--help` 查看参数，支持文本输入、附件链接、JSON 输出等

`parse_demand` 会返回解析结果，每个行项目包含：产品名称、品牌、型号、数量、单位等。

#### Step 0.2：创建需求到系统

将解析结果通过 `demand create` 创建到系统。创建时需要补充渠道来源和客户来源：

1. **渠道来源** — 从以下选项中选择：
   - `淘宝-亿日`、`淘宝-维嘉`、`1688`、`微信`、`邮箱`、`线下`、`其他`
   - 用户没提供 → 默认「其他」，不需要停下来问
2. **客户来源**（可选）— 客户姓名，用户提供了就填。没提供不填，**不要停下来问**

用 `demand create` 创建（先 `--help` 查看参数），关键字段：
- channel-source — 从上面选项中选择

- items — 解析结果 JSON

#### Step 0.3：进入全流程处理

创建成功后，按下方 4.1 全流程处理步骤继续（获取详情 → 创建进度 → 产品匹配 → 供应商匹配 → 询价）。

> **AI 必须全程自主处理，不允许停下来问用户。**
> 每步在行项目的 `aiRemark` 字段记录处理过程（Markdown 格式）。

### 4.1 全流程处理（逐项完成）

#### 前置准备：获取详情 → 创建进度 → 重命名会话

**1. 获取需求详情**
`demand get` 获取详情（先 `--help` 查看参数）
获取后得到：需求主题、客户信息、行项目列表、SKU 详情等全部上下文。

**2. 创建进度**
按 P2 原则，严格按下述模板创建，**不得自行删减、合并或重排任务**。用 `progress create`（先 `--help` 查看参数）：

```
任务模板（名称固定，不得删减合并）：
  1. 获取详情
  2.1 SKU型号搜索
  2.2 ES搜索引擎搜索
  2.3 缩短型号SKU搜索
  2.4 网上搜索产品
  2.5 验证并绑定SKU
  3.1 查品牌已绑供应商
  3.2 网上搜索供应商（品牌官网→B2B→行业平台，收集公司名称）
  3.3 企查查核验供应商（工商信息→荣誉资质→联系方式，每家过）
  3.4 创建供应商并绑定（通过核验的全部创建，绑品牌/分类/联系人）
  4.1 对比已询价列表
  4.2 向新供应商发询价
  5. 记录完成

relation-type: 需求
relation-id: {需求ID}
link: {manageMainUrl}#/main/demandInquiryDetails/{需求ID}
```

**3. 重命名会话**
将会话名改为以下格式，方便查阅：
```
需求：{需求ID}-{需求主题}
```

#### 进度更新规则

进度在开始时一次性建好全部任务。**每执行一个操作，就立即更新一次进度**，不能攒到一批再做：

```bash
# ⚠️ 正确做法：每完成一个小步骤就更新
产品匹配步骤 → 立即 progress step
下一个产品搜索 → 立即 progress step

# ❌ 错误做法：全部做完才更新
全部搜索完 → 一次性更新多个进度
```

更新格式：用 `progress step`（先 `--help` 查看参数）
- summary — 记录实际操作中的思考和结果，即使没有也要如实记录（想了什么办法、查了什么、为什么没找到）
- links — 相关来源链接，多个逗号分隔

---

**Step 1：产品搜索与信息采集**

> 目标：确认产品真实存在、获取完整规格信息、决定是否绑定。搜索只为信息采集，不是一定要绑到已有 SKU。

#### 搜索策略（按优先级，换方式不等于不存在，多种都要尝试）

**一、系统内搜索（先查有没有现成的）**

- **A. SKU 型号搜索** — 用 `product sku list`（先 `--help` 查看参数），用客户型号搜 model 字段
- **B. ES 搜索（线上环境）** — 用 `product spu es-search`（先 `--help` 查看参数），搜型号/品牌+型号。仅线上可用
- **C. 缩短型号再试** — "CVS100N" → "CVS100"，"RA/8125/50" → "RA/8125"

**系统搜不到 → 开始网上搜索。**

#### 搜索关键词方法论（重要）

**不要一次把客户所有描述堆进搜索框。** 拆成短关键词组合，每次搜 2-3 个核心词，多轮尝试。

以客户说"超声波换能器 用于气体，压敏陶瓷做的，频率 200K"为例：

**第一轮（核心词）：**
- `超声波换能器 200K` — 产品名 + 关键规格
- `200KHz ultrasonic transducer` — 英文更精准
- `超声波换能器 气体` — 产品名 + 用途

**第二轮（换角度，第一轮结果不够时）：**
- `200KHz 换能器 厂家` — 找生产商
- `ultrasonic air transducer 200KHz` — 英文换表达
- `压电陶瓷 超声波 气体` — 材质+产品+用途

**第三轮（搜到品牌后，针对品牌深挖）：**
- `聚德电子 DYA-200 datasheet` — 品牌+系列+文档
- `Unictron A200M7 spec` — 品牌+型号+规格

**原则：短词优先，多轮尝试，搜不到就换词。** 中文搜不到换英文，英文搜不到换拼音。不要一次丢 5 个关键词进去。

**二、先确定品牌（客户没给品牌时必须先做这一步）**

客户没指定品牌 → **不能跳过品牌直接搜产品**。必须先找到这个产品由哪些品牌生产：

- 搜 `{产品名} {型号} 厂家` / `{产品名} {型号} 品牌` / `{产品名} {型号} 生产商`
- 搜英文：`{产品名} {型号} manufacturer` / `{产品名} {型号} brand`
- 用 `web_fetch` 打开找到的页面，列出所有生产这个产品的品牌
- **至少找到 2-3 个品牌后，再进入下一步（逐个品牌去官网验证）**

**三、品牌官网产品页（逐个品牌验证）**

对上面找到的每个品牌，去官网确认产品是否存在：

- 搜 `{品牌} 官网` → 打开官网 → 找 **"产品中心"、"型号查询"、"Product Finder"、"产品目录"、"Downloads"**
- 搜 `{品牌} {型号}` → 直接定位产品详情页
- 从产品页提取：完整型号、规格参数表、技术参数、尺寸图、产品图片
- 官网列的所有规格信息**全部记录**
- **如果官网上找不到这个型号 → 说明这个品牌可能不生产这个产品，标记为"待确认"**

**四、技术文档 / 规格书 / 选型手册**

- 搜 `{品牌} {型号} datasheet pdf` / `{型号} 规格书` / `{型号} 选型手册`
- 搜 `{品牌} {系列名} catalog` / `{品牌} 产品目录 pdf`
- 搜 `{品牌} {型号} 说明书` / `{品牌} {型号} 安装手册`
- 提取：规格表、尺寸图、性能曲线、选型配置表（有规格变化时的全部选项）

**五、B2B 平台 + 搜索引擎交叉验证**

- 1688/百度爱采购：搜 `{品牌} {型号}` → 看详情页参数和实物图
- icspec.com / allchips.com：电子元器件专用
- 搜索引擎：`{品牌} {型号} 参数` / `{型号} 替代` / `{品牌} {型号} 技术参数`

**搜索词多换：** 完整型号 → 缩短型号 → 品牌+产品名 → 型号单独 → 英文 → 拼音 → datasheet → catalog → 选型 → 参数 → 替代 → 说明书

**每次搜到结果必须用 web_fetch 实际打开页面查看内容，不能只看搜索摘要。**

#### 交叉验证与判断

找到产品信息后，**必须逐一对比验证**，不能凭感觉下结论：

1. **客户需求 vs 搜索结果**：客户说的型号/规格/用途，和找到的产品是否一致？逐条对比
2. **品牌确认**：找到的品牌是官方品牌还是经销商自己标的？→ 以官网为准
3. **规格一致**：频率 200K、压敏陶瓷、气体用 → 找到的产品规格是否每一项都对得上？
4. **至少 2-3 个独立来源**确认后，才能下结论

**在 aiRemark 中必须写明验证推理过程：**
- 客户要求什么 → 实际搜到了什么 → 每一项是否匹配 → 为什么确认/排除
- 不能只贴链接，要写清楚"为什么这个链接里的产品就是客户要的"

#### 确认标准

| 情况 | 操作 |
|------|------|
| 同品牌 + 同型号完全一致 | 绑定已有 SKU |
| 客户没指定品牌，搜到 WKEA 同型号 | 绑定 |
| 客户没指定品牌，搜到其他品牌（Festo/SMC等） | **不绑定**。只记 aiRemark，上架时按客户原文创建，品牌默认 WKEA |
| 品牌或型号不完全一致 | 只记 aiRemark，不绑定 |
| 搜不到 | 如实记录，继续供应商匹配 |

> 搜索是信息采集，不是自动绑定。系统搜不到 → 网上搜。搜不到在系统内的 → 继续往下走，供应商报价后可确认产品存在再上架。

#### 上架并绑定

**系统已有匹配 SKU** — 用 `demand update-item`（先 `--help` 查看参数）绑定

**系统没有** — 用 `demand simple-create-product` 一键上架，或手动 `product spu create` + `product sku create`

#### aiRemark 记录规范（强制）

**每搜索一次就记录一次，不能攒到最后一起写。** 业务人员可能不熟悉产品领域，AI 必须详细解释推理过程，让人看得懂"为什么这个产品就是客户要的"。

每个行项目的 aiRemark 必须包含以下部分。**最前面必须先写结论**，让业务人员一眼看到关键信息再往下看细节：

**⓪ 结论（最前面，先写这个）**
- 最终确认的产品名、型号、品牌
- 客户未指定品牌 → 标注"系统默认 WKEA"
- 参考价格范围（如有）
- 信息来源（列出最关键的产品页 URL）
- 一句话总结：产品是否存在、下一步做什么

**① 品牌发现过程**
- 搜了什么关键词找品牌（厂家 / 品牌 / manufacturer）
- 找到了哪些品牌生产这个产品（至少列出 2-3 个）
- 每个品牌的官网地址
- 为什么认为这些品牌是相关的

**② 逐个品牌验证过程**
- 对每个品牌，在官网上搜了什么、找到了什么产品页
- 产品页原文 URL（必须附上）
- 产品页上的完整规格参数
- 是否有 datasheet / 选型手册 / 产品目录 / PDF（附链接）

**③ 客户需求 vs 搜索结果逐条对比（核心）**
- 客户说的每一项 → 搜到的产品对应项 → 是否一致 → 推理依据
- 产品名、型号、规格、用途、材质、频率、尺寸……逐条比
- 不能只写"匹配"，要解释为什么匹

格式参考：

```markdown
## 结论
- 产品名：超声波换能器 | 型号：200KHz DYA-200 / A200M7 等
- 品牌：客户未指定 → 系统默认 WKEA
- 参考价格：DigiKey A200M7 约 ¥699-844 / AT200 约 $48
- 来源：https://www.unictron.com/.../a200m7/ | http://judepzt.com/...
- 结论：5 个品牌确认生产，规格匹配 → 继续供应商匹配

## 产品匹配

### 品牌发现
- 搜 "超声波换能器 200KHz 厂家" → 找到：
  - 潍坊聚德电子 (http://judepzt.com) — 官网有 DYA-200 系列
  - Unictron (https://www.unictron.com) — 官网有 A200M7
  - 杭州安布雷拉 (https://www.ywj5188.com) — 官网有 DYA-200 系列

### 逐个验证

#### 聚德电子 DYA-200-01GX
- 官网产品页：http://judepzt.com/teamview_4826261.html
- 规格：200KHz±5%、钛合金外壳、IP68、气体流量计专用
- 需求对比：
  - 频率 200K → DYA-200-01GX 标注 200KHz±5% → ✅ 匹配
  - 气体用 → 产品页写明"气体流量计用" → ✅ 匹配
  - 压敏陶瓷 → 聚德官网说明采用压电陶瓷(PZT) → ✅ 匹配

#### Unictron A200M7
- 官网产品页：https://www.unictron.com/product/a200m7-200-khz-air-transducer/
- 规格：200KHz、钛合金、IP68、1.6MPa
- 需求对比：
  - 频率 200K → A200M7 标注 200KHz → ✅ 匹配
  - 气体用 → 产品名含 "Air Transducer" → ✅ 匹配
  - 压敏陶瓷 → 产品分类在 Piezoelectric Ceramics 下 → ✅ 匹配

### 结论
- 3 个品牌确认生产 200KHz 气体超声波换能器，规格匹配客户需求
- 系统内无匹配 SKU → 上架时默认 WKEA，内部记录溯源
- → 继续供应商匹配
```

→ 向供应商询价 → 供应商报价 → 转订单。

### 实体关系
```
DemandQuotation（需求询价主表）
  ├── DemandQuotationItem（行项目 1..N）
  │     └── skuId → SKU（可选绑定）
  ├── DemandQuotationDocInfo（供应商报价记录）
  │     └── DemandQuotationDocInfoData（报价明细）
```

### 状态
| 状态码 | 含义 |
|--------|------|
| 274 | 待处理 |
| 275 | 处理中 |
| 276 | 已完成 |
| 291 | 已取消 |

### 行项目关键字段
- 客户填写（只读）：`productName`、`productBrand`、`productModel`
- 后台编辑：`manageProductName`、`manageProductBrand`、`manageProductModel`
- 绑定产品：`skuId`
- 业务数据：`quantity`、`expectPrice`、`finalSkuPrice`、`grossMargin`
- 记录：`aiRemark`（AI 处理记录，Markdown 格式）

---

## 2. 前置条件

- 产品匹配前：需求已被当前 AI 领取（领取由 AI 自动处理）
- 供应商匹配前：行项目必须已绑定 SKU
- 询价前：行项目涉及的品牌必须有绑定的供应商

---

## 3. 判断依据

- **ES 搜索可用**（线上） → 优先用 ES 搜索已有产品
- **ES 不可用**（开发环境返回 500） → 走 WebSearch
- **品牌有供应商** → 直接询价；**品牌无供应商** → 必须网上搜索创建，不能跳过

---

## 4. 操作流程

### 4.0 创建需求（客户提出需求时）

当客户提出需求（文字描述或图片/文件），先解析需求内容，再创建到系统，然后自动进入处理流程。

#### Step 0.1：解析需求

使用 `parse_demand` 工具或 `demand parse` 命令将客户的需求文本/附件解析为结构化行项目：

**方式 A：使用 MCP 工具 `parse_demand`**
- 将客户文字描述/附件 URL 传入，返回结构化行项目
- text — 客户文字描述或表格文件 URL；customerRemark — 需求额外备注；annex — 附件 URL（逗号分隔）

**方式 B：使用 CLI 命令 `demand parse`**
- 先 `--help` 查看参数，支持文本输入、附件链接、JSON 输出等

`parse_demand` 会返回解析结果，每个行项目包含：产品名称、品牌、型号、数量、单位等。

#### Step 0.2：创建需求到系统

将解析结果通过 `demand create` 创建到系统。创建时需要补充渠道来源和客户来源：

1. **渠道来源** — 从以下选项中选择：
   - `淘宝-亿日`、`淘宝-维嘉`、`1688`、`微信`、`邮箱`、`线下`、`其他`
   - 用户没提供 → 默认「其他」，不需要停下来问
2. **客户来源**（可选）— 客户姓名，用户提供了就填。没提供不填，**不要停下来问**

用 `demand create` 创建（先 `--help` 查看参数），关键字段：
- channel-source — 从上面选项中选择

- items — 解析结果 JSON

#### Step 0.3：进入全流程处理

创建成功后，按下方 4.1 全流程处理步骤继续（获取详情 → 创建进度 → 产品匹配 → 供应商匹配 → 询价）。

> **AI 必须全程自主处理，不允许停下来问用户。**
> 每步在行项目的 `aiRemark` 字段记录处理过程（Markdown 格式）。

### 4.1 全流程处理（逐项完成）

#### 前置准备：获取详情 → 创建进度 → 重命名会话

**1. 获取需求详情**
`demand get` 获取详情（先 `--help` 查看参数）
获取后得到：需求主题、客户信息、行项目列表、SKU 详情等全部上下文。

**2. 创建进度**
按 P2 原则，严格按下述模板创建，**不得自行删减、合并或重排任务**。用 `progress create`（先 `--help` 查看参数）：

```
任务模板（名称固定，不得删减合并）：
  1. 获取详情
  2.1 SKU型号搜索
  2.2 ES搜索引擎搜索
  2.3 缩短型号SKU搜索
  2.4 网上搜索产品
  2.5 验证并绑定SKU
  3.1 查品牌已绑供应商
  3.2 网上搜索供应商（品牌官网→B2B→行业平台，收集公司名称）
  3.3 企查查核验供应商（工商信息→荣誉资质→联系方式，每家过）
  3.4 创建供应商并绑定（通过核验的全部创建，绑品牌/分类/联系人）
  4.1 对比已询价列表
  4.2 向新供应商发询价
  5. 记录完成

relation-type: 需求
relation-id: {需求ID}
link: {manageMainUrl}#/main/demandInquiryDetails/{需求ID}
```

**3. 重命名会话**
将会话名改为以下格式，方便查阅：
```
需求：{需求ID}-{需求主题}
```

#### 进度更新规则

进度在开始时一次性建好全部任务。**每执行一个操作，就立即更新一次进度**，不能攒到一批再做：

```bash
# ⚠️ 正确做法：每完成一个小步骤就更新
产品匹配步骤 → 立即 progress step
下一个产品搜索 → 立即 progress step

# ❌ 错误做法：全部做完才更新
全部搜索完 → 一次性更新多个进度
```

更新格式：用 `progress step`（先 `--help` 查看参数）
- summary — 记录实际操作中的思考和结果，即使没有也要如实记录（想了什么办法、查了什么、为什么没找到）
- links — 相关来源链接，多个逗号分隔

---

**Step 1：产品搜索与信息采集**

> 目标：确认产品真实存在、获取完整规格信息、决定是否绑定。搜索只为信息采集，不是一定要绑到已有 SKU。

#### 搜索策略（按优先级，换方式不等于不存在，多种都要尝试）

**一、系统内搜索（先查有没有现成的）**

- **A. SKU 型号搜索** — 用 `product sku list`（先 `--help` 查看参数），用客户型号搜 model 字段
- **B. ES 搜索（线上环境）** — 用 `product spu es-search`（先 `--help` 查看参数），搜型号/品牌+型号。仅线上可用
- **C. 缩短型号再试** — "CVS100N" → "CVS100"，"RA/8125/50" → "RA/8125"

**二、先确定品牌（客户没给品牌时必须先做这一步）**

客户没指定品牌 → **不能跳过品牌直接搜产品**。必须先找到这个产品由哪些品牌生产：

- 搜 `{产品名} {型号} 厂家` / `{产品名} {型号} 品牌` / `{产品名} {型号} 生产商`
- 搜英文：`{产品名} {型号} manufacturer` / `{产品名} {型号} brand`
- 用 `web_fetch` 打开找到的页面，列出所有生产这个产品的品牌
- **至少找到 2-3 个品牌后，再进入下一步（逐个品牌去官网验证）**

**三、品牌官网产品页（逐个品牌验证）**

对上面找到的每个品牌，去官网确认产品是否存在：

- 搜 `{品牌} 官网` → 打开官网 → 找 **"产品中心"、"型号查询"、"Product Finder"、"产品目录"、"Downloads"**
- 搜 `{品牌} {型号}` → 直接定位产品详情页
- 从产品页提取：完整型号、规格参数表、技术参数、尺寸图、产品图片
- 官网列的所有规格信息**全部记录**
- **如果官网上找不到这个型号 → 说明这个品牌可能不生产这个产品，标记为"待确认"**

**四、技术文档 / 规格书 / 选型手册**

- 搜 `{品牌} {型号} datasheet pdf` / `{型号} 规格书` / `{型号} 选型手册`
- 搜 `{品牌} {系列名} catalog` / `{品牌} 产品目录 pdf`
- 搜 `{品牌} {型号} 说明书` / `{品牌} {型号} 安装手册`
- 提取：规格表、尺寸图、性能曲线、选型配置表（有规格变化时的全部选项）

**五、B2B 平台 + 搜索引擎交叉验证**

- 1688/百度爱采购：搜 `{品牌} {型号}` → 看详情页参数和实物图
- icspec.com / allchips.com：电子元器件专用
- 搜索引擎：`{品牌} {型号} 参数` / `{型号} 替代` / `{品牌} {型号} 技术参数`

**搜索词多换：** 完整型号 → 缩短型号 → 品牌+产品名 → 型号单独 → 英文 → 拼音 → datasheet → catalog → 选型 → 参数 → 替代 → 说明书

**每次搜到结果必须用 web_fetch 实际打开页面查看内容，不能只看搜索摘要。**

#### 交叉验证与判断

找到产品信息后，**必须逐一对比验证**，不能凭感觉下结论：

1. **客户需求 vs 搜索结果**：客户说的型号/规格/用途，和找到的产品是否一致？逐条对比
2. **品牌确认**：找到的品牌是官方品牌还是经销商自己标的？→ 以官网为准
3. **规格一致**：频率 200K、压敏陶瓷、气体用 → 找到的产品规格是否每一项都对得上？
4. **至少 2-3 个独立来源**确认后，才能下结论

**在 aiRemark 中必须写明验证推理过程：**
- 客户要求什么 → 实际搜到了什么 → 每一项是否匹配 → 为什么确认/排除
- 不能只贴链接，要写清楚"为什么这个链接里的产品就是客户要的"

#### 确认标准

| 情况 | 操作 |
|------|------|
| 同品牌 + 同型号完全一致 | 绑定已有 SKU |
| 客户没指定品牌，搜到 WKEA 同型号 | 绑定 |
| 客户没指定品牌，搜到其他品牌（Festo/SMC等） | **不绑定**。只记 aiRemark，上架时按客户原文创建，品牌默认 WKEA |
| 品牌或型号不完全一致 | 只记 aiRemark，不绑定 |
| 搜不到 | 如实记录，继续供应商匹配 |

> 搜索是信息采集，不是自动绑定。系统搜不到 → 网上搜。搜不到在系统内的 → 继续往下走，供应商报价后可确认产品存在再上架。

#### 上架并绑定

**系统已有匹配 SKU** — 用 `demand update-item`（先 `--help` 查看参数）绑定

**系统没有** — 用 `demand simple-create-product` 一键上架，或手动 `product spu create` + `product sku create`

#### aiRemark 记录规范（强制）

**每搜索一次就记录一次，不能攒到最后一起写。** 业务人员可能不熟悉产品领域，AI 必须详细解释推理过程，让人看得懂为什么这个产品就是客户要的。

每个行项目的 aiRemark 必须包含以下五部分：

**① 品牌发现过程**
- 搜了什么关键词找品牌（厂家 / 品牌 / manufacturer）
- 找到了哪些品牌生产这个产品（至少列出 2-3 个）
- 每个品牌的官网地址
- 为什么认为这些品牌是相关的

**② 逐个品牌验证过程**
- 对每个品牌，在官网上搜了什么、找到了什么产品页
- 产品页原文 URL（必须附上）
- 产品页上的完整规格参数
- 是否有 datasheet / 选型手册 / 产品目录 / PDF（附链接）

**③ 客户需求 vs 搜索结果逐条对比（核心）**
- 客户说的每一项 → 搜到的产品对应项 → 是否一致 → 推理依据
- 产品名、型号、规格、用途、材质、频率、尺寸……逐条比
- 不能只写匹配，要解释为什么匹

格式参考：

\
→ 向供应商询价 → 供应商报价 → 转订单。

### 实体关系
```
DemandQuotation（需求询价主表）
  ├── DemandQuotationItem（行项目 1..N）
  │     └── skuId → SKU（可选绑定）
  ├── DemandQuotationDocInfo（供应商报价记录）
  │     └── DemandQuotationDocInfoData（报价明细）
```

### 状态
| 状态码 | 含义 |
|--------|------|
| 274 | 待处理 |
| 275 | 处理中 |
| 276 | 已完成 |
| 291 | 已取消 |

### 行项目关键字段
- 客户填写（只读）：`productName`、`productBrand`、`productModel`
- 后台编辑：`manageProductName`、`manageProductBrand`、`manageProductModel`
- 绑定产品：`skuId`
- 业务数据：`quantity`、`expectPrice`、`finalSkuPrice`、`grossMargin`
- 记录：`aiRemark`（AI 处理记录，Markdown 格式）

---

## 2. 前置条件

- 产品匹配前：需求已被当前 AI 领取（领取由 AI 自动处理）
- 供应商匹配前：行项目必须已绑定 SKU
- 询价前：行项目涉及的品牌必须有绑定的供应商

---

## 3. 判断依据

- **ES 搜索可用**（线上） → 优先用 ES 搜索已有产品
- **ES 不可用**（开发环境返回 500） → 走 WebSearch
- **品牌有供应商** → 直接询价；**品牌无供应商** → 必须网上搜索创建，不能跳过

---

## 4. 操作流程

### 4.0 创建需求（客户提出需求时）

当客户提出需求（文字描述或图片/文件），先解析需求内容，再创建到系统，然后自动进入处理流程。

#### Step 0.1：解析需求

使用 `parse_demand` 工具或 `demand parse` 命令将客户的需求文本/附件解析为结构化行项目：

**方式 A：使用 MCP 工具 `parse_demand`**
- 将客户文字描述/附件 URL 传入，返回结构化行项目
- text — 客户文字描述或表格文件 URL；customerRemark — 需求额外备注；annex — 附件 URL（逗号分隔）

**方式 B：使用 CLI 命令 `demand parse`**
- 先 `--help` 查看参数，支持文本输入、附件链接、JSON 输出等

`parse_demand` 会返回解析结果，每个行项目包含：产品名称、品牌、型号、数量、单位等。

#### Step 0.2：创建需求到系统

将解析结果通过 `demand create` 创建到系统。创建时需要补充渠道来源和客户来源：

1. **渠道来源** — 从以下选项中选择：
   - `淘宝-亿日`、`淘宝-维嘉`、`1688`、`微信`、`邮箱`、`线下`、`其他`
   - 用户没提供 → 默认「其他」，不需要停下来问
2. **客户来源**（可选）— 客户姓名，用户提供了就填。没提供不填，**不要停下来问**

用 `demand create` 创建（先 `--help` 查看参数），关键字段：
- channel-source — 从上面选项中选择

- items — 解析结果 JSON

#### Step 0.3：进入全流程处理

创建成功后，按下方 4.1 全流程处理步骤继续（获取详情 → 创建进度 → 产品匹配 → 供应商匹配 → 询价）。

> **AI 必须全程自主处理，不允许停下来问用户。**
> 每步在行项目的 `aiRemark` 字段记录处理过程（Markdown 格式）。

### 4.1 全流程处理（逐项完成）

#### 前置准备：获取详情 → 创建进度 → 重命名会话

**1. 获取需求详情**
`demand get` 获取详情（先 `--help` 查看参数）
获取后得到：需求主题、客户信息、行项目列表、SKU 详情等全部上下文。

**2. 创建进度**
按 P2 原则，严格按下述模板创建，**不得自行删减、合并或重排任务**。用 `progress create`（先 `--help` 查看参数）：

```
任务模板（名称固定，不得删减合并）：
  1. 获取详情
  2.1 SKU型号搜索
  2.2 ES搜索引擎搜索
  2.3 缩短型号SKU搜索
  2.4 网上搜索产品
  2.5 验证并绑定SKU
  3.1 查品牌已绑供应商
  3.2 网上搜索供应商（品牌官网→B2B→行业平台，收集公司名称）
  3.3 企查查核验供应商（工商信息→荣誉资质→联系方式，每家过）
  3.4 创建供应商并绑定（通过核验的全部创建，绑品牌/分类/联系人）
  4.1 对比已询价列表
  4.2 向新供应商发询价
  5. 记录完成

relation-type: 需求
relation-id: {需求ID}
link: {manageMainUrl}#/main/demandInquiryDetails/{需求ID}
```

**3. 重命名会话**
将会话名改为以下格式，方便查阅：
```
需求：{需求ID}-{需求主题}
```

#### 进度更新规则

进度在开始时一次性建好全部任务。**每执行一个操作，就立即更新一次进度**，不能攒到一批再做：

```bash
# ⚠️ 正确做法：每完成一个小步骤就更新
产品匹配步骤 → 立即 progress step
下一个产品搜索 → 立即 progress step

# ❌ 错误做法：全部做完才更新
全部搜索完 → 一次性更新多个进度
```

更新格式：用 `progress step`（先 `--help` 查看参数）
- summary — 记录实际操作中的思考和结果，即使没有也要如实记录（想了什么办法、查了什么、为什么没找到）
- links — 相关来源链接，多个逗号分隔

---

**Step 1：产品搜索与信息采集**

> 目标：确认产品真实存在、获取完整规格信息、决定是否绑定。搜索只为信息采集，不是一定要绑到已有 SKU。

#### 搜索策略（按优先级，换方式不等于不存在，多种都要尝试）

**一、系统内搜索（先查有没有现成的）**

- **A. SKU 型号搜索** — 用 `product sku list`（先 `--help` 查看参数），用客户型号搜 model 字段
- **B. ES 搜索（线上环境）** — 用 `product spu es-search`（先 `--help` 查看参数），搜型号/品牌+型号。仅线上可用
- **C. 缩短型号再试** — "CVS100N" → "CVS100"，"RA/8125/50" → "RA/8125"

**二、先确定品牌（客户没给品牌时必须先做这一步）**

客户没指定品牌 → **不能跳过品牌直接搜产品**。必须先找到这个产品由哪些品牌生产：

- 搜 `{产品名} {型号} 厂家` / `{产品名} {型号} 品牌` / `{产品名} {型号} 生产商`
- 搜英文：`{产品名} {型号} manufacturer` / `{产品名} {型号} brand`
- 用 `web_fetch` 打开找到的页面，列出所有生产这个产品的品牌
- **至少找到 2-3 个品牌后，再进入下一步（逐个品牌去官网验证）**

**三、品牌官网产品页（逐个品牌验证）**

对上面找到的每个品牌，去官网确认产品是否存在：

- 搜 `{品牌} 官网` → 打开官网 → 找 **"产品中心"、"型号查询"、"Product Finder"、"产品目录"、"Downloads"**
- 搜 `{品牌} {型号}` → 直接定位产品详情页
- 从产品页提取：完整型号、规格参数表、技术参数、尺寸图、产品图片
- 官网列的所有规格信息**全部记录**
- **如果官网上找不到这个型号 → 说明这个品牌可能不生产这个产品，标记为"待确认"**

**四、技术文档 / 规格书 / 选型手册**

- 搜 `{品牌} {型号} datasheet pdf` / `{型号} 规格书` / `{型号} 选型手册`
- 搜 `{品牌} {系列名} catalog` / `{品牌} 产品目录 pdf`
- 搜 `{品牌} {型号} 说明书` / `{品牌} {型号} 安装手册`
- 提取：规格表、尺寸图、性能曲线、选型配置表（有规格变化时的全部选项）

**五、B2B 平台 + 搜索引擎交叉验证**

- 1688/百度爱采购：搜 `{品牌} {型号}` → 看详情页参数和实物图
- icspec.com / allchips.com：电子元器件专用
- 搜索引擎：`{品牌} {型号} 参数` / `{型号} 替代` / `{品牌} {型号} 技术参数`

**搜索词多换：** 完整型号 → 缩短型号 → 品牌+产品名 → 型号单独 → 英文 → 拼音 → datasheet → catalog → 选型 → 参数 → 替代 → 说明书

**每次搜到结果必须用 web_fetch 实际打开页面查看内容，不能只看搜索摘要。**

#### 交叉验证与判断

找到产品信息后，**必须逐一对比验证**，不能凭感觉下结论：

1. **客户需求 vs 搜索结果**：客户说的型号/规格/用途，和找到的产品是否一致？逐条对比
2. **品牌确认**：找到的品牌是官方品牌还是经销商自己标的？→ 以官网为准
3. **规格一致**：频率 200K、压敏陶瓷、气体用 → 找到的产品规格是否每一项都对得上？
4. **至少 2-3 个独立来源**确认后，才能下结论

**在 aiRemark 中必须写明验证推理过程：**
- 客户要求什么 → 实际搜到了什么 → 每一项是否匹配 → 为什么确认/排除
- 不能只贴链接，要写清楚"为什么这个链接里的产品就是客户要的"

#### 确认标准

| 情况 | 操作 |
|------|------|
| 同品牌 + 同型号完全一致 | 绑定已有 SKU |
| 客户没指定品牌，搜到 WKEA 同型号 | 绑定 |
| 客户没指定品牌，搜到其他品牌（Festo/SMC等） | **不绑定**。只记 aiRemark，上架时按客户原文创建，品牌默认 WKEA |
| 品牌或型号不完全一致 | 只记 aiRemark，不绑定 |
| 搜不到 | 如实记录，继续供应商匹配 |

> 搜索是信息采集，不是自动绑定。系统搜不到 → 网上搜。搜不到在系统内的 → 继续往下走，供应商报价后可确认产品存在再上架。

#### 上架并绑定

**系统已有匹配 SKU** — 用 `demand update-item`（先 `--help` 查看参数）绑定

**系统没有** — 用 `demand simple-create-product` 一键上架，或手动 `product spu create` + `product sku create`

#### aiRemark 记录规范（强制）

**每搜索一次就记录一次，不能攒到最后一起写。** 每个行项目的 aiRemark 必须包含：

- 搜索了什么关键词 / 打开了什么链接（**原文 URL 必须保留**）
- 找到了什么结果（型号、规格、参数、文档链接）
- 为什么这样判断（确认 / 排除 / 不确定）
- 下一步决定

格式参考（Markdown）：

\`\`\`markdown
## 产品匹配

### 系统搜索
- 关键词：R3874237-AL → product sku list，0 条

### 网上搜索
- 搜 "Festo R3874237-AL datasheet" → 确认官网 https://www.festo.com/... 产品页存在
- 搜 "Festo RA/8000 系列 catalog" → 选型手册 https://... 确认缸径/行程参数
- B2B 交叉验证：1688 https://... 参数一致

### 规格信息
- 品牌：Festo | 型号：R3874237-AL | 缸径：125mm | 行程：50mm
- 接口：G1/2 | 活塞杆：32mm | 推力：7363N@6bar

### 结论
- 品牌+型号从 3 个来源确认一致 → 系统内无匹配 SKU → 继续供应商匹配
\`\`\`

**注意：** `aiRemark` 要写得像项目笔记——记录思考过程、每一步操作、每个来源链接和网址。不要只写结论不写过程。

---

**Step 2：供应商匹配**

> 目标：尽可能多地找到合格供应商，不能只找一家。只要通过核验的全部创建。
> **供应商开发始终用客户原始信息**（行项目的 productName / productBrand），不是系统匹配后的数据。客户没指定品牌 → 按 WKEA 品牌去开发供应商。

**A. 系统查询** — 用 `demand vendors-by-brand`（先 `--help` 查看参数），默认只返回主要供应商

**B. 品牌无供应商或不够** — 查看 `vendor guide`，按供应商开发流程从网上搜索并创建。搜索策略、企查查核验流程、决策标准、创建步骤均在 vendor 模块文档中。

**C. 创建绑定后** — 继续进入询价

核验结果和创建记录写入 `aiRemark`，格式参考进度总结。

> **验证通过**（品牌+型号完全一致）→ 绑定 SKU，进入下一步。
> **近似匹配**（品牌或型号有细微差异，如少字母、拼写接近）→ **不能直接当完全匹配处理**。必须先验证：
>   - 网上搜索确认：用搜索引擎搜该品牌/产品，打开官网或产品页确认是否正确
>   - 多渠道交叉验证：至少 2-3 个独立来源确认信息一致
>   - 验证后确认一致的 → 按完全匹配处理，在 aiRemark 中记录验证过程
>   - 验证后仍不确定的 → 不能强行绑定，继续尝试其他搜索方式


```markdown
## 产品匹配
- 搜索方式：SPU名称搜索 / SKU型号搜索 / WebSearch
- 关键词：施耐德断路器 CVS100N
- 思考过程：SPU搜到系列产品但没到型号级别，换SKU搜型号看看
- 操作：
  1. ✏️ product spu list --keyword 施耐德断路器 → 找到SPU-108（系列SPU，未精确到型号）
  2. ✏️ product sku list --keyword CVS100N → 找到SKU W001234，品牌施耐德，型号CVS100N完全一致
- 来源验证：
  - [品牌官网] https://www.se.com/cn/product/CVS100N → 型号、规格确认一致
  - [B2B平台] https://www.1688.com/... → 同型号产品，参数一致
  - [技术文档] https://www.se.com/cn/download/CVS100N-datasheet → 数据手册确认
- 验证结论：品牌施耐德✅ 型号CVS100N✅ 规格参数一致✅
- 结果：已绑定 SKU W001234（完全匹配）

## 供应商匹配
- 品牌：3M
- 搜索方式：系统查询（品牌已绑供应商）
- 思考过程：查3M品牌绑了哪些供应商，发现S00123是主要供应商，还有S00456
- 操作：
  1. ✏️ demand vendors-by-brand --brand-id 3M → S00123(主要), S00456
  2. ✏️ 对比已询价列表 → S00123未询价
- 来源：
  - [供应商详情] https://admin.wkea.cn/#/main/supplier-detail/S00123
  - [企查查] https://agent.qcc.com/query?company=3M中国 → 经营存续，无风险
- 验证结果：官网有联系方式，企查查工商状态正常
- 结果：已向S00123发送询价
```

**注意：** `aiRemark` 要写得像项目笔记——记录思考过程、每一步操作、每个来源链接和网址。不要只写结论不写过程。

---

**Step 3：向供应商询价**

```
**Step 3.1** — 用 `demand vendors-by-brand` 查已绑供应商。**不加 --all**，默认只返回主供应商

**Step 3.2** — demand get 结果已含 quotedVendors，无需重复调用

**Step 3.3** — AI 对比两个列表，差集 = 需要新发询价的供应商

**Step 3.4** — 用 `demand quote-to-vendor` 对差集中的供应商逐个发询价
```

**询价规则（重要）：**
- **只用 `vendors-by-brand` 返回的供应商，不要自己加。** 默认只推主供应商。没有任何主供应商时，再加 `--all` 推全部
- AI 在供应商开发阶段搜到的供应商，先创建绑定到品牌，下次 `vendors-by-brand` 自然能查到
- 每次 `quote-to-vendor` 只对一个供应商，多个就多次调用
- 询价后调 `quoted-vendors` 确认已发，结果记到 aiRemark

**Step 4：生成最终报告**

全部流程完成后，读取 `docs/report-template.html` 模板，从以下数据源提取内容填充：

| 数据 | 来源命令 |
|------|---------|
| 需求基本信息 | `demand get --id {ID}` |
| 行项目列表 | `demand items --demand-id {ID}` |
| 进度时间线 | `progress get --id {进度ID}` |
| 产品研究内容 | 行项目的 `aiRemark` 字段 |
| 供应商信息 | 进度 step summary + vendor get |
| 询价情况 | `demand get --id {ID}` 的 quotedVendors |

**模板使用规则：模板的 HTML 结构和 CSS 样式完全不动，只替换 `{{占位符}}` 的内容。** 占位符包括：

| 占位符 | 填充内容 |
|--------|---------|
| `{{DEMAND_TOPIC}}` | 需求主题 |
| `{{DEMAND_ID}}` | 需求 ID |
| `{{CUSTOMER_NAME}}` | 客户名称 |
| `{{STATUS}}` | 需求状态 |
| `{{FINISH_TIME}}` | 处理完成时间 |
| `{{PRODUCT_ROWS}}` | 每个行项目一行。产品研究结果列包含：上方 mini-table 列出找到的品牌/型号/规格/链接，下方 `<div class="ai-cell">` 放入该行项目的 **aiRemark 完整原文**（max-height:380px 超出滚动）。供应商列列出核验后的供应商标签。报价列标注询价状态 |
| `{{TIMELINE_ITEMS}}` | 进度步骤列表 |
| `{{LINKS}}` | 跳转链接列表 |

**产品结果列的嵌套表格格式：**
```html
<table class="mini-table">
<tr><td><b>杭州安布雷拉</b></td><td>DYA-200-01VG</td><td>200KHz±5%</td><td>PZT</td><td>-</td><td><a href="...">产品页</a></td></tr>
<tr><td><b>Unictron</b></td><td>A200M7</td><td>200KHz</td><td>PZT</td><td>¥699-844</td><td><a href="...">产品页</a></td></tr>
</table>
```

**供应商列的格式：**
```html
<span class="tag ok">S00860 安布雷拉（存续/5专利）</span>
<span class="tag ok">S00862 嘉康（高新+专精特新）</span>
```

生成后将 HTML 写入行项目的 aiRemark（追加在现有内容后面，用 `---` 分隔），同时告知用户可以通过后台查看。

### 4.2 供应商报价与价格管理

> 以下为业务参考，不属于全流程处理的固定步骤。AI 根据实际场景按需查阅使用。

#### 查看报价详情
- 用 `demand vendor-quotes`（先 `--help` 查看参数）

#### 报价评估与建议

当业务人员说"看看报价情况"时，AI 应主动整理报价数据并给出建议：

**1. 展示报价对比**
- 用 `demand vendor-quotes` 拉取全部供应商报价
- 将数据整理为对比表格：供应商名称 | 资质 | 单价 | 交期 | 库存 | 发货地 | 报价总金额 | 是否完成
- 同时展示 `demand vendor-quotes` 结果中的供应商资质（tags）、报价有效期等辅助信息

**2. 给出采纳建议**
- 评估标准（按优先级）：价格低 > 交期短 > 已有采购记录 > 供应商资质（专精特新/高新企业等优先）
- 综合排序后推荐 1-2 家最合适的供应商，说明理由
- 明确告知：**所有报价均会保存，业务员只需决定采纳谁**

**3. 等待业务员决策**
- 问"你想采纳哪家供应商？"或直接指定供应商 ID
- 业务员确定后，执行以下操作：

**4. 保存全部报价 + 设置采纳**
- 所有供应商报价 → 用 `demand save-price` 逐条保存（保留全部报价记录）
- 采纳的供应商 → 用 `product supply set-master` 设为主供应商价格

#### 保存供应商报价

**记录报价（不改售价）** — 用 `demand save-price`（先 `--help` 查看参数），isMaster=false

**设为主供应商价格** — 用 `product supply set-master`（先 `--help` 查看参数）
- 改写 SKU 默认售价、采购价、交期，同步更新维嘉替代品折扣
- **智能重定向：** 如果该 SKU 已绑定完全替代品（isFullReplace），价格自动设到替代品上

#### 替代品与停产

以下工具各自独立，按需组合。命令均需先 `--help` 查看参数。

**替代关系** — `product sku replace`
- `add` / `list` / `remove` 子命令。完全替代加 full-replace 参数。只管两个 SKU 关系，不管价格和停产。

**创建替代产品** — `product quick-create` 或 `spu create` + `sku create`

**停产标记** — `product spu update` 的停产参数。值 0=停产无替代，SPU ID=停产有替代。

**操作示例：**
- 供应商说"第N个产品停产，用xxx替代" → items 定位 itemId/skuId → quick-create 建替代品 → sku replace add 绑关系 → set-master 设价（自动走到替代品）→ spu update 停产参数
- 供应商说"可用xxx替代，没停产" → sku replace add 绑关系 → set-master 设价
- 供应商说"停产，没替代" → spu update 停产参数值 0


### 4.3 执行记录规范

每次搜索和操作都在 aiRemark 中记录，格式如下：

```markdown
## 产品匹配
- 搜索方式：SPU名称搜索
- 关键词：3M 电动钻
- 系统结果：找到 SPU W001234，名称匹配
- 型号验证：客户型号"3M-DRILL-600W" vs 系统型号"3M-DRILL-600W" → 完全一致
- 结果：已绑定 SKU W001234

## 供应商匹配
- 品牌：3M
- 搜索方式：系统查询（品牌已绑供应商）
- 找到供应商：3M中国有限公司（邮箱: sales@3m.cn, 电话: 021-12345678）
- 企查查验证：统一信用代码 91310000XXXX，经营状态存续
- 结果：已创建供应商 S00xxx，已绑定品牌 3M
```

---

### 4.4 行项目管理
- 新增：`demand add-item`（先 `--help` 查看参数）
- 批量新增：`demand item batch-add`
- 修改：`demand update-item`（先 `--help` 查看参数）
- 删除：`demand delete-item`

---

## 5. 边界情况

- **ES 不可用**（开发环境返回 500）→ 必须走 WebSearch，不能跳过产品匹配
- **品牌无供应商** → 必须网上搜索创建，不能报错退出
- **供应商没有邮箱/电话** → 不能创建，继续深挖找到联系方式
- **多个供应商匹配** → 优先选有授权证书、工商状态正常、有官网的
- **供应商异步报价** → AI 发出询价后等待供应商响应（异步），需轮询检查报价状态
- **一键上架** — 内部自动查重（按品牌+型号），重复则直接绑定
- **主供应商机制** — 品牌可设置主要供应商，自动询价时优先询价
